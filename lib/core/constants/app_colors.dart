import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary — Teal/Green brand Jastip Wirosari
  static const Color primary = Color(0xFF00897B);
  static const Color primaryLight = Color(0xFF4DB6AC);
  static const Color primaryDark = Color(0xFF00695C);

  // Secondary — Amber/Orange accent
  static const Color secondary = Color(0xFFFF8F00);
  static const Color secondaryLight = Color(0xFFFFB300);
  static const Color secondaryDark = Color(0xFFE65100);

  // Backgrounds
  static const Color background = Color(0xFFF8FFFE);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF0F7F6);

  // Text
  static const Color textPrimary = Color(0xFF1A2E2C);
  static const Color textSecondary = Color(0xFF5C7C79);
  static const Color textHint = Color(0xFF9FBDBA);

  // Status colors
  static const Color success = Color(0xFF2E7D32);
  static const Color error = Color(0xFFD32F2F);
  static const Color warning = Color(0xFFF57C00);
  static const Color info = Color(0xFF0277BD);

  // Order status colors
  static const Color statusPending = Color(0xFFFF8F00);
  static const Color statusConfirmed = Color(0xFF0277BD);
  static const Color statusShopping = Color(0xFF6A1B9A);
  static const Color statusDelivery = Color(0xFF00838F);
  static const Color statusDelivered = Color(0xFF2E7D32);
  static const Color statusCancelled = Color(0xFFD32F2F);

  // Divider & border
  static const Color divider = Color(0xFFE0EEEC);
  static const Color border = Color(0xFFCCE5E2);

  // Shimmer
  static const Color shimmerBase = Color(0xFFE8F5F3);
  static const Color shimmerHighlight = Color(0xFFF5FFFE);
}
